extends layout

block content
    h1=title

    ul
    each genre in genre_list
      li
        a(href=genre.url) #{genre.name}
                
    else
      li There are no authors
    hr
    p 
      a.btn.btn-info(href='/catalog/genre/create') Create new genre